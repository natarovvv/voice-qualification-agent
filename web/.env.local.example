# Use wss:// anywhere but localhost - a ws:// call puts the microphone audio,
# the transcript and the caller's email on the wire in clear text.
NEXT_PUBLIC_WS_URL=ws://localhost:8000

# Must match AUTH_TOKEN on the server. Leave blank for local development.
NEXT_PUBLIC_WS_TOKEN=
